services:
  backend:
    build:
      context: ./backend
    container_name: ai-carrer-advisor-backend
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      